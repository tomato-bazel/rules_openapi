load("@gazelle//:def.bzl", "gazelle")

# gazelle:prefix github.com/tomato-bazel/rules_openapi
# gazelle:go_naming_convention import
gazelle(name = "gazelle")

package(default_visibility = ["//visibility:public"])

exports_files([
    "MODULE.bazel",
    "Cargo.toml",
    "Cargo.lock",
])
